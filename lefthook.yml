pre-commit:
  parallel: true
  jobs:
    - name: rubocop
      glob: "**/*.rb"
      run: mise exec -- bin/rubocop {staged_files}

    - name: typecheck
      glob: "**/*.{ts,tsx}"
      run: mise exec -- npx tsc --noEmit

pre-push:
  parallel: true
  jobs:
    - name: brakeman
      run: mise exec -- bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error

    - name: bundler-audit
      run: mise exec -- bin/bundler-audit
