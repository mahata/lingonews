import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "dev.fly.lingonews",
  appName: "LingoNews",
  webDir: "www",
  server: {
    url: "https://lingonews.fly.dev",
  },
};

export default config;
